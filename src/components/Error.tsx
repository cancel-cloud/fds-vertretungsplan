import React from 'react';

const Error: React.FC = () => {
    return (
        <div className="flex items-center justify-center min-h-screen">
            <p className="text-red-500 text-xl">Das liegt an FDS</p>
        </div>
    );
};

export default Error;
